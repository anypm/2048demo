stages:
- name: build-stage
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: example-2048demox:${CICD_EXECUTION_SEQUENCE}
      pushRemote: true
      registry: quay.io/anypm
timeout: 60
